program media_aritmetica;
var
  a,b,c:integer;
  d:real;
begin
    writeln('introduceti a=');
    read(a);
    writeln('introduceti b=');
    read(b);
    writeln('introduceti c=');
    read(c);
    d:=(a+b+c)/3;
    writeln('media aritmetica este:',d:5:2);
    read;
end.
     